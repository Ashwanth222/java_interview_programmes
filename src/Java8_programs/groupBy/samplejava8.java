package Java8_programs.groupBy;

import java.util.stream.Collectors;

public class samplejava8 {
    public static void main(String[] args){
//        The following will accumulate strings into a List:
//        List<String> asList = stringStream.collect(Collectors.toList());
//        The following will classify Person objects by city:
//        Map<String, List<Person>> peopleByCity      =
//                personStream.collect(Collectors.groupingBy(Person::getCity));
//        The following will classify Person objects by state and city,
//        cascading two Collectors together:
//        Map<String, Map<String, List<Person>>> peopleByStateAndCity      = personStream
//                .collect(Collectors.groupingBy(Person::getState,  Collectors.groupingBy(Person::getState))                                             Collectors.groupingBy(Person::getCity)));
    }

}
